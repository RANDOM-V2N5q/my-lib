<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/bottom_navigation"
    app:startDestination="@id/books">
    <fragment
        android:id="@+id/books"
        android:name="com.example.mylib.BooksList"
        android:label="fragment_books_list"
        tools:layout="@layout/fragment_books_list" />
    <fragment
        android:id="@+id/games"
        android:name="com.example.mylib.GamesList"
        android:label="fragment_games_list"
        tools:layout="@layout/fragment_games_list" />
    <fragment
        android:id="@+id/movies"
        android:name="com.example.mylib.MoviesList"
        android:label="fragment_movies_list"
        tools:layout="@layout/fragment_movies_list" />
</navigation>